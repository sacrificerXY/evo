#pragma once

#include "neat/genome.h"

namespace neat
{

class Mutator
{
public:
    //Genome add_connection(const Genome& g, NodeIndex from, NodeIndex to);
    //Genome add_node(const Genome& g, NodeIndex from, NodeIndex to);
};
    

}